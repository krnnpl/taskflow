[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "cd /app/frontend && npm install"
]

[phases.build]
cmds = [
  "cd /app/frontend && npm run build"
]

[start]
cmd = "node server.js"